---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile

helmDefaults:
  cleanupOnFail: true
  wait: true
  waitForJobs: true

# cilium, coredns, external-secrets, secretstore, flux-operator, flux-instance
#--context {{ requiredEnv "cluster" }}
releases:
  #- name: cilium
  #  namespace: kube-system
  #  atomic: false
  #  chart: oci://quay.io/cilium/charts/cilium
  #  version: 1.18.6
  #  values: ['../../templates/values.yaml.gotmpl']
  #  hooks:
  #    - # Wait for cilium CRDs to be available
  #      events: ['postsync']
  #      command: bash
  #      args:
  #        - -c
  #        - until kubectl --context={{ requiredEnv "cluster" }} get crd ciliumloadbalancerippools.cilium.io ciliumbgpadvertisements.cilium.io ciliumbgppeerconfigs.cilium.io ciliumbgpclusterconfigs.cilium.io &>/dev/null; do sleep 5; done
  #      showlogs: true
  #    - # Advertise Kubernetes VIP
  #      events: ['postsync']
  #      command: kubectl
  #      args:
  #        - --context={{ requiredEnv "cluster" }}
  #        - apply
  #        - --namespace=kube-system
  #        - --server-side
  #        - --field-manager=kustomize-controller
  #        - --filename={{ requiredEnv "cluster_apps" }}/kube-system/cilium/networking.yaml
  #      showlogs: true
  #- name: coredns
  #  namespace: kube-system
  #  atomic: true
  #  chart: oci://ghcr.io/coredns/charts/coredns
  #  version: 1.45.2
  #  values: ['../../templates/values.yaml.gotmpl']
  #  needs: ['kube-system/cilium']
  - name: cert-manager
    namespace: cert-manager
    atomic: true
    chart: oci://quay.io/jetstack/charts/cert-manager
    version: v1.19.3
    values: ['../../templates/values.yaml.gotmpl']
  #  needs: ['kube-system/coredns']



  #- name: external-secrets
  #  namespace: external-secrets
  #  chart: oci://ghcr.io/external-secrets/charts/external-secrets
  #  version: 1.3.1
  #  values:
  #    - ../../templates/values.yaml.gotmpl
  #  needs:
  #    - cert-manager/cert-manager

  - name: flux-operator
    namespace: flux-system
    atomic: true
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator
    version: 0.40.0
    values: ['../../templates/values.yaml.gotmpl']
    needs: ['cert-manager/cert-manager']

  - name: flux-instance
    namespace: flux-system
    atomic: true
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
    version: 0.40.0
    values: ['../../templates/flux-values.yaml.gotmpl']
    needs: ['flux-system/flux-operator']

  #- name: flux-operator
  #  namespace: flux-system
  #  atomic: true
  #  chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator
  #  version: 0.40.0
  #  values: ['../../templates/values.yaml.gotmpl']
  #  needs: ['external-secrets/external-secrets']

  #- name: flux-instance
  #  namespace: flux-system
  #  atomic: true
  #  chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
  #  version: 0.40.0
  #  values: ['../../templates/flux-values.yaml.gotmpl']
  #  needs: ['flux-system/flux-operator']






  #- name: flux-operator
  #  namespace: flux-system
  #  atomic: true
  #  chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator
  #  version: 0.40.0
  #  values: ['{{ requiredEnv "shared_apps" }}/flux-system/flux-operator/helm/values.yaml']
  #  needs: ['security/external-secrets']
  #- name: flux-instance
  #  namespace: flux-system
  #  atomic: true
  #  chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
  #  version: 0.40.0
  #  values: ['{{ requiredEnv "cluster_dir" }}/flux-instance/helm/values.yaml']
  #  needs: ['flux-system/flux-operator']



  #cluster: '{{.cluster | default "main"}}'
  #cluster_apps: '{{.ROOT_DIR}}/kubernetes/apps/{{.cluster}}'
  #cluster_dir: '{{.ROOT_DIR}}/kubernetes/clusters/{{.cluster}}'
  #cluster_config_dir: '{{.ROOT_DIR}}/kubernetes/config/{{.cluster}}'
  #shared_apps: '{{.ROOT_DIR}}/kubernetes/apps/base'

  #- name: cilium
  #  namespace: kube-system
  #  chart: oci://quay.io/cilium/charts/cilium
  #  version: 1.18.6
  #  values:
  #    - ../../templates/values.yaml.gotmpl
  #  hooks:
  #    - # Wait for Cilium CRDs to be available
  #      command: bash
  #      args:
  #        - -c
  #        - until kubectl get crd ciliumloadbalancerippools.cilium.io ciliuml2announcementpolicies.cilium.io ciliumbgpadvertisements.cilium.io ciliumbgppeerconfigs.cilium.io ciliumbgpclusterconfigs.cilium.io &>/dev/null; do sleep 5; done
  #      events:
  #        - postsync
  #      showlogs: true
  #    - # Apply Cilium Resources
  #      command: kubectl
  #      args:
  #        - apply
  #        - --namespace=kube-system
  #        - --server-side
  #        - --field-manager=kustomize-controller
  #        - --filename=../../../kubernetes/apps/main/kube-system/cilium/app/networking.yaml
  #      events:
  #        - postsync
  #      showlogs: true
