---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: llm
components:
  - ../../../components/common
  - ../../../vars/main/cluster
replacements:
  - path: ../../../components/replacements/ks.yaml
resources:
  - ./searxng.yaml
  - ./open-webui.yaml
  # paperless-gpt and co

