---
# yaml-language-server: $schema=https://schemas.sstr.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app radarr
spec:
  components:
    - ../../../../components/envoy/oidc/authentik/main
  interval: 1h
  path: ./kubernetes/apps/base/download/radarr
  postBuild:
    substitute:
      APP: *app
      APP_UPPER: RADARR
      APP_SLUG: radarr
      CLUSTER: ${CLUSTER}
      GATEWAY: envoy-external
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
      - kind: Secret
        name: cluster-secrets
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false

