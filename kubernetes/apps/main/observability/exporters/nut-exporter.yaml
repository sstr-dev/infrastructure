---
# yaml-language-server: $schema=https://schemas.sstr.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app nut-exporter
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  interval: 1h
  #components:
  #  - ../../../../../components/envoy/ext-auth
  path: ./kubernetes/apps/base/observability/exporters/nut-exporter
  prune: true
  retryInterval: 2m
  postBuild:
    substitute:
      APP: *app
      GATEWAY: "envoy-internal"
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  timeout: 5m
  wait: true
---
# yaml-language-server: $schema=https://schemas.sstr.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app nut-exporter-probes
spec:
  dependsOn:
    - name: nut-exporter
  path: ./kubernetes/apps/${CLUSTER}/observability/nut-exporter-probes
  prune: true
  postBuild:
    substitute:
      APP: *app
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  interval: 30m
  retryInterval: 1m
  timeout: 5m
